namespace = empirates

# The Daughter's Ballad anomaly trigger
ship_event = {
	id = empirates.1
	title = empirates.1.name
	desc = empirates.1.desc
	picture = GFX_evt_star_neutron
	show_sound = event_ship_bridge
	location = FROM
	trackable = yes

	is_triggered_only = yes

	immediate = {
		from = { clear_deposits = yes }
	}

	option = {
		name = empirates.1.a
		from = { create_archaeological_site = site_daughters_ballad_dig }
	}
}